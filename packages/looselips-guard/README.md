# looselips-guard

Stops your coding agent publishing your data to the world.

```bash
npm install -g looselips-guard   # needs python3 on PATH
```

Then point your host's PreToolUse hook at `looselips-guard`. Full docs:
https://github.com/ed-is-ai/looselips-guard

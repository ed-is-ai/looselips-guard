{
  "version": 1,
  "hooks": {
    "PreToolUse": [
      {
        "type": "command",
        "bash": "looselips-guard",
        "matcher": "bash|shell|.+-(?:create|post|send|comment|publish|upload|write|update|append|patch)\\b.*"
      }
    ]
  }
}
